# Fixed (moved)

This folder no longer stores active source-of-truth specs.

Former files were moved to `docs/concepts/old/` as legacy archives.

Use instead:

- `stam-rt-lib/docs/*`
- `primitives/docs/*`
- `docs/architecture/*`
