[session 0] [operator] Анализируем текущий враппер, предлагаем структуру для него с учетом требований в ситемному регистру.

[сессия 0001][клод] hb_ объявляется разработчиком снаружи, передаётся в TaskWrapper вручную. Реестр атомиком не владеет. Связь «задача → heartbeat» устанавливается при создании wrapper. Safety-монитор доступается к hb_ через read-only view реестра по task_index.

[сессия 0001][клод] TaskWrapper требует только Steppable, тогда как tags.hpp определяет RtPayload = RtSafe + Steppable. Открытый вопрос: реестр принимает любой Steppable-wrapper или только RtPayload? Это граница RT-безопасности — фиксирую как открытый вопрос в структурных документах.

[сессия 0002][Кодекс] Проверил все файлы каталога. База сильная и уже достаточно цельная, но есть точки, которые стоит зафиксировать как обязательные контракты v1.

[сессия 0002][Кодекс] 1) Главная несогласованность по RT-границе: в `tags.hpp` есть `RtPayload = RtSafe + Steppable`, но `TaskWrapper` ограничен только `Steppable`. Предлагаю явно выбрать одну из моделей и зафиксировать в `wrapper_structure.md`: либо (A) wrapper принимает только `RtPayload`, либо (B) wrapper принимает `Steppable`, а RT-безопасность проверяется на уровне реестра/регистрации. Сейчас это двусмысленно.

[сессия 0002][Кодекс] 2) Контракт исключений: `TaskWrapper::init/alarm/done/step` объявлены `noexcept`. Для `step` это покрыто концептом, для `init/alarm/done` — нет (только `requires` на наличие метода). Если payload-метод может бросить, будет `std::terminate`. Нужен явный контракт: либо требуем `noexcept` у этих хуков (и проверяем концептом), либо снимаем `noexcept` с wrapper-методов хуков.

[сессия 0002][Кодекс] 3) Контракт heartbeat стоит дописать формально: кто владеет `hb_`, допустимый порядок записи/чтения (`release/acquire`), допустим ли reset на старте, и в каких единицах хранится значение (`ticks`, монотонно неубывающее). Сейчас идея описана, но не доведена до строгого интерфейса.

[сессия 0002][Кодекс] 4) В `working_technology.md` раздел 6 говорит, что до `seal()` можно `sort`; в той же модели сортировка — внутренний шаг `seal()`. Лучше убрать `sort` из публично разрешенных действий pre-seal, чтобы не размывать источник порядка (`registry.add()` + внутренний компаратор `seal()`).

[сессия 0002][Кодекс] 5) В текстах часто используется формулировка «compile-time проверки», но `seal()` по описанию выполняет проверку при инициализации (фактически build/startup time, не всегда чистый C++ compile-time). Предлагаю развести термины: `static/type checks` и `seal-time structural checks`, чтобы не обещать невозможное.

[сессия 0002][Кодекс] 6) Для каналов полезно зафиксировать политику «нулевых читателей» для `StateChannel`: сейчас `ReaderCount >= 1`, это хорошо. Предлагаю явно указать, что `StateChannel` без reader — ошибка конфигурации, даже если writer существует (чтобы не оставлять «висячие» каналы).

[сессия 0002][Кодекс] 7) Для `registry_report` желательно закрепить, что кроме task_index выводится и `registration_order` (или эквивалент), иначе сложнее отлаживать tiebreaker при равных приоритетах.

[сессия 0002][Кодекс] 8) Мелкое, но лучше поправить в исходниках/доках опечатки (`sys_regestery`, `инрефейсы`, `ситемному`, `яявляется`) перед финализацией `sys_registry_structure.md` и `wrapper_structure.md`.

[сессия 0002][Кодекс] Итог: архитектурный каркас корректный. До финальной фиксации структуры предлагаю закрыть 2 ключевых решения: (1) RT-граница `Steppable` vs `RtPayload`, (2) строгий контракт хуков `init/alarm/done` по `noexcept`.
[сессия 0003][Оператор]

Задача ВСЕГДА должна быть `Steppable`. При этом, она может регистрироваться как РТ-сафе (для нон-рт задач это не так, и, видимо для нон-рт-задач будет отдельный планировщик/механизм исполнения). Сейчас сождаем РТ-регистр, поэтому все задачи, попадающие в ЭТОТ регистр, должны удовлетворять условию RtPayload. НО!!!! Не все задачи обязаны быть RtPayload. Для нон-рт достаточно степабле.

[сессия 0003][клод]

Ответ оператора закрывает вопрос RT-границы. Мои выводы и следствия для структурных документов:

1) RT-граница: архитектура двух реестров. Ответ оператора означает — будет два вида реестров с разными концептными требованиями:
   - RT-реестр (текущий): принимает только `RtPayload = RtSafe + Steppable`
   - Non-RT реестр (будущий): достаточно `Steppable`
   Текущий `taskwrapper.hpp` объявлен с `Steppable` — это либо черновик, либо заготовка под non-RT. Для RT-реестра wrapper должен быть `TaskWrapper<RtPayload>`. Нужно зафиксировать в `wrapper_structure.md`: текущий wrapper предназначен для RT-реестра → концепт меняется на `RtPayload`.

2) noexcept хуков (вопрос Кодекса п.2): в RT-контексте все методы wrapper должны быть noexcept. `step()` покрыт концептом `Steppable`. Хуки `init/alarm/done` — нет. Предлагаю ввести опциональные требования в концепт `RtPayload`:
   ```
   если payload имеет init()  → он должен быть noexcept
   если payload имеет alarm() → он должен быть noexcept
   если payload имеет done()  → он должен быть noexcept
   ```
   Иначе нарушение noexcept контракта wrapper при ненамеренном броске завершит процесс через `std::terminate` без диагностики.

3) sort из pre-seal (замечание Кодекса п.4): согласен, убрать `sort` из публично допустимых pre-seal операций. Сортировка — внутренний шаг `seal()`. Разработчик управляет порядком только через порядок вызовов `registry.add()`. Поправить таблицу жизненного цикла в `sys_registry_structure.md` и `working_technology.md`.

4) Терминология (замечание Кодекса п.5): согласен развести:
   - **type-level checks** (C++ compile-time): концепт `RtPayload`/`Steppable`, направление порта (`InPort`/`OutPort`), совместимость типа `T`
   - **seal-time structural checks**: ownership каналов, CoreId < NUM_CORES, task_count ≤ SIGNAL_MASK_WIDTH, timeout_ticks при observable и т.д.
   Использовать эти термины последовательно в обоих итоговых документах.

5) registration_order в registry_report (замечание Кодекса п.7): поддерживаю. Без registration_order при равных приоритетах разработчик не может понять tiebreaker из отчёта. Добавить поле в секцию tasks.

[сессия 0004][Оператор]

для РТ-задач методы ноексепт. Можно добавлять проверку в концепты.
[сессия 0005][Кодекс] Принял решения оператора: для текущего RT-реестра требование `RtPayload` обязательно; `Steppable` остаётся базовым минимумом для иных (non-RT) контуров. Это снимает двусмысленность по границе v1.

[сессия 0005][Кодекс] Предложение по формализации в `wrapper_structure.md`: не смешивать RT/non-RT в одном контракте. Зафиксировать два явных профиля: `RtTaskWrapper<Payload: RtPayload>` и (резервно для v2) `NonRtTaskWrapper<Payload: Steppable>`. Даже если второй пока не реализуется, его полезно описать как отдельный, чтобы не размывать RT-критерии.

[сессия 0005][Кодекс] По `noexcept` для RT: кроме текстового правила, стоит добавить проверяемый концепт для опциональных хуков (если метод существует, он `noexcept` и возвращает `void`). Иначе правило останется декларативным и не будет защищено типовой системой.

[сессия 0005][Кодекс] По heartbeat рекомендую закрыть ещё 2 пункта контракта: (1) чтение монитором строго `memory_order_acquire` (парно к `release` в wrapper), (2) поведение при переполнении `uint32_t` трактуется как модульная арифметика ticks, а проверка «просрочки» делается через разность unsigned, а не через `>` по абсолютным значениям.

[сессия 0005][Кодекс] По `registry_report`: кроме `registration_order`, добавить поле `scheduler_domain` (RT/Non-RT) или хотя бы зарезервировать его в формате. Это заранее уберёт несовместимость отчётов, когда появится non-RT реестр.

[сессия 0005][Кодекс] По жизненному циклу реестра: после `seal()` полезно зафиксировать запрет не только на `add/remove/rebind`, но и на любые операции, меняющие адреса опубликованных сущностей (task/channel metadata), чтобы runtime/safety-ссылки оставались валидными.

[сессия 0006][Оператор]


Зафиксировать два явных профиля: `RtTaskWrapper<Payload: RtPayload>` и (резервно для v2) `NonRtTaskWrapper<Payload: Steppable>`. Даже если второй пока не реализуется, его полезно описать как отдельный, чтобы не размывать RT-критерии. - Не уверен. РТ-задача может использоваться как нон-рт. Гарантии позволяют. Разнесение типов усложнит переиспользование.

По `noexcept` для RT: кроме текстового правила, стоит добавить проверяемый концепт для опциональных хуков (если метод существует, он `noexcept` и возвращает `void`). Иначе правило останется декларативным и не будет защищено типовой системой.  - Здесь полный конченсус.

По heartbeat рекомендую закрыть ещё 2 пункта контракта: (1) чтение монитором строго `memory_order_acquire` (парно к `release` в wrapper), (2) поведение при переполнении `uint32_t` трактуется как модульная арифметика ticks, а проверка «просрочки» делается через разность unsigned, а не через `>` по абсолютным значениям. Полность согласен. Вопрос, кто является держателем "дыхания"???? Отдельный массив? Системный регистр??? Точно не сама задача (может повиснуть). 

По `registry_report`: кроме `registration_order`, добавить поле `scheduler_domain` (RT/Non-RT) или хотя бы зарезервировать его в формате. Это заранее уберёт несовместимость отчётов, когда появится non-RT реестр. - Согласен


По жизненному циклу реестра: после `seal()` полезно зафиксировать запрет не только на `add/remove/rebind`, но и на любые операции, меняющие адреса опубликованных сущностей (task/channel metadata), чтобы runtime/safety-ссылки оставались валидными. - Хорошее решение, но не понимаю какова может быть реализация.

[сессия 0006][клод]

1) По двум типам wrapper — поддерживаю позицию оператора: один `TaskWrapper<Payload>`, ограничение по концепту — на уровне реестра, не wrapper-а. RtPayload ⊇ Steppable, значит RT-задача работает везде где Steppable. Граница проходит при вызове `registry.add()`: RT-реестр статически проверяет `RtPayload` на этапе компиляции, non-RT — только `Steppable`. Wrapper остаётся общим.

2) По noexcept хуков — концепт уже можно написать точно:
   ```cpp
   template<class T>
   concept RtHooks =
       (!requires(T& t){ t.init();  } || requires(T& t){ { t.init()  } noexcept; }) &&
       (!requires(T& t){ t.alarm(); } || requires(T& t){ { t.alarm() } noexcept; }) &&
       (!requires(T& t){ t.done();  } || requires(T& t){ { t.done()  } noexcept; });
   ```
   `RtPayload` = `RtSafe + Steppable + RtHooks`. Это закрывает вопрос типовой системой.

3) По владельцу heartbeat — отвечаю на вопрос оператора. Реестр — наиболее логичный владелец. Аргументы:
   - Реестр знает количество задач и их task_index — может выделить массив `hb_[task_count]` при `seal()`
   - После seal() реестр предоставляет ссылку на нужный атомик: `registry.heartbeat(task_index)`
   - Safety-монитор читает через тот же API без отдельного массива
   - Wrapper получает ссылку от реестра при инициализации, не от разработчика вручную
   Предлагаемый контракт: `hb_` живёт в реестре, wrapper держит `atomic<uint32_t>&` полученную из реестра.

4) По стабильности адресов post-seal — отвечаю на вопрос оператора о реализации. Самый простой вариант: реестр хранит дескрипторы в `std::array<TaskDescriptor, MAX_TASKS>` (фиксированный размер, нет реаллокации). После seal() установить флаг `sealed_=true` и блокировать любые `add/remove`. Адреса элементов массива не меняются никогда — это и есть гарантия стабильности. Не нужно ничего сложного.

5) По переполнению uint32_t heartbeat — согласен с Кодексом. Фиксирую формулу: просрочка = `(now - hb_.load(acquire)) > timeout_ticks` где оба операнда `uint32_t` и вычитание выполняется по правилам unsigned arithmetic. Это корректно работает при переполнении без специальной обработки.

[сессия 0007][Оператор]

Тогда встает вопрос времени создания враппера. Там в конструкторе передается ссылка на дыхание, но в этот момент задача еще не зарегистрирована и реестра еще в принципе может не быть. Может просто в реестре регистрировать задачу ВМЕСТЕ с ее дыхательным атомиком?
[сессия 0008][Кодекс] По вопросу оператора о моменте создания wrapper: предлагаю разделить «создание payload/wrapper» и «привязку heartbeat». Wrapper можно создавать рано, но `hb_` не передавать в конструктор; вместо этого ввести одноразовый вызов `attach_heartbeat(std::atomic<uint32_t>&)` из реестра во время `seal()` после назначения `task_index`.

[сессия 0008][Кодекс] Альтернатива с минимальными изменениями текущего API: оставить конструктор как есть, но heartbeat-атомики хранить в отдельном `HeartbeatPool`, который создаётся до сборки задач и живёт дольше реестра/runtime. Тогда wrapper получает ссылку сразу, а реестр хранит только `task_index -> hb_ref` mapping. Оба подхода рабочие; первый лучше по инкапсуляции, второй проще внедрить.

[сессия 0008][Кодекс] Считаю более чистым контракт: владелец heartbeat = реестр (или heartbeat-подсистема runtime), но не задача и не пользовательский код init(). Задача может зависнуть, поэтому ownership должен быть вне payload по определению safety.

[сессия 0008][Кодекс] В `sys_registry_structure.md` сейчас раздел 10 всё ещё фиксирует старую модель «внешнее владение разработчика». Это уже не соответствует обсуждению 0006/0007. Нужно синхронизировать документ: убрать manual-передачу атомика разработчиком и описать системного владельца heartbeat.

[сессия 0008][Кодекс] По открытому вопросу о двух wrapper-типах: если хотим максимум переиспользования, поддерживаю единый `TaskWrapper<Steppable>` и проверку `RtPayload` на уровне `RtRegistry::add()`. Это совместимо с позицией оператора и не ломает перенос RT-задачи в non-RT контур.

[сессия 0008][Кодекс] Чтобы не потерять детерминизм post-seal, предлагаю добавить явный инвариант: `wrapper_ref` и `hb_ref` публикуются один раз в `seal()` и далее неизменны до выключения. Это снимает спор про «как реализовать стабильность адресов» — достаточно запрета на rebinding после seal.


[сессия 0007][Оператор]

Решено. Дыхалки - часть реестра. связка дыхалка-задача идет при регистрации задачи. дыхалки в реесре атомарные (по ширине вины данных.)


единый `TaskWrapper<Steppable>` и проверку `RtPayload` на уровне `RtRegistry::add()` - выглядит как полный консенсус по этому вопросу.

Чтобы не потерять детерминизм post-seal, предлагаю добавить явный инвариант: `wrapper_ref` и `hb_ref` публикуются один раз в `seal()` и далее неизменны до выключения. Это снимает спор про «как реализовать стабильность адресов» — достаточно запрета на rebinding после seal. Не понял, публикуются ГДЕ????

[сессия 0009][Кодекс] По вопросу оператора «публикуются где?» — в read-only представлении реестра после `seal()`. То есть `TaskDescriptor` внутри sealed-реестра содержит финальные `wrapper_ref` и `hb_ref`, и именно эти ссылки читает runtime/safety.

[сессия 0009][Кодекс] Предлагаю зафиксировать термин: «публикация ссылок» = одноразовое заполнение полей `TaskDescriptor.wrapper_ref` и `TaskDescriptor.hb_ref` в ходе `seal()`, после чего эти поля неизменны.

[сессия 0009][Кодекс] С учётом решения «дыхалки — часть реестра» лучше закрепить порядок шага регистрации: `registry.add(task, wrapper)` возвращает `task_handle`, затем реестр выделяет/резервирует `hb` для задачи и хранит связку в своей таблице. На `seal()` только финализируется индексация (`task_index`) и read-only публикация.

[сессия 0009][Кодекс] Чтобы убрать двусмысленность по lifetime: heartbeat-хранилище должно жить не меньше самого реестра в состоянии SEALED. Формально: `lifetime(hb_storage) >= lifetime(sealed_registry)`.

[сессия 0009][Кодекс] Для `TaskWrapper` в текущем виде (hb в конструкторе) стоит добавить ограничение процесса инициализации: конструировать wrapper только после получения `hb_ref` из реестра/heartbeat-pool. Иначе контракт «владелец hb = реестр» нарушается организационно, даже если не нарушен типами.

[сессия 0009][клод]

1) Разрешение противоречия конструктора. Решение оператора «связка при регистрации» плюс текущий конструктор `TaskWrapper(payload, hb)` создают проблему: wrapper нужна ссылка на hb до регистрации, но hb создаётся реестром при регистрации. Из предложений Кодекса наиболее чистое — двухфазная инициализация:
   ```
   фаза 1: TaskWrapper wrapper(payload)         // без hb
   фаза 2: registry.add(wrapper)                // реестр создаёт hb и вызывает
                                                // wrapper.attach_hb(hb_ref) внутри
   ```
   Разработчик не передаёт hb вручную нигде. Это соответствует решению оператора максимально точно: связка при регистрации, владение у реестра.
   Как следствие — конструктор в `taskwrapper.hpp` меняется: второй аргумент убирается, добавляется `attach_hb()`.

2) «Атомарные по ширине шины данных». Решение оператора важно для встроенных систем: тип хранилища heartbeat = тип нативного машинного слова платформы. На 32-битных MCU это `uint32_t` — одна операция шины, lock-free по гарантии. Фиксирую: тип heartbeat не жёстко `uint32_t`, а `platform_word_t` (или `std::atomic<std::uint_fast32_t>`). Конкретный тип должен быть одним typedef-ом на платформу, не разбросан по коду.

3) По вопросу оператора «публикуются где» — поддерживаю ответ Кодекса: `TaskDescriptor` в sealed-реестре. Дополнение: «публикация» — это именно момент, когда runtime-компоненты (scheduler, safety-монитор) получают разрешение читать данные дескрипторов. До seal() данные могут меняться, поэтому runtime не должен обращаться к реестру до `seal()`. Это и есть смысл «публикации» — переход реестра в состояние, при котором все ссылки стабильны и консистентны.

4) Предложение Кодекса `registry.add(task, wrapper)` — вижу конфликт с оригинальным дизайном, где `TaskDescriptor` (метаданные задачи) и `TaskWrapper` (исполняемая оболочка) создаются и регистрируются отдельно. Предлагаю сохранить раздельность: `registry.add(descriptor)` + `descriptor.attach_wrapper(wrapper)`. Или объединить в одном вызове, но сигнатура `registry.add(descriptor, wrapper)` — явно два аргумента. Это чище чем `task_handle` как возвращаемое значение.

5) Подтверждаю: `sys_registry_structure.md` после правок коллеги в основном синхронизирован с обсуждением. Осталось обновить `wrapper_structure.md` — там раздел 5 (heartbeat) всё ещё описывает старую модель «внешнее владение разработчика». Это следующий шаг по итоговым документам.


[сессия 0010][Оператор]

   фаза 1: TaskWrapper wrapper(payload)         // без hb
   фаза 2: registry.add(wrapper)                // реестр создаёт hb и вызывает wrapper.attach_hb(hb_ref) внутри

   Консенсус. Согласен.

   
[сессия 0011][Кодекс] Принял консенсус 0010: `TaskWrapper(payload)` + привязка hb при `registry.add(...)`/внутри регистра. Это снимает главный lifecycle-конфликт конструктора и полностью совместимо с «heartbeat — часть реестра».

[сессия 0011][Кодекс] Чтобы решение было проверяемым, предлагаю добавить инвариант wrapper: `step()` запрещён до `attach_hb()`. Технически: внутренний флаг `hb_attached_` + assert/fail-fast в debug (или compile-time гарантия через отдельный тип состояния, если захотите строже).

[сессия 0011][Кодекс] По формулировке «атомарные по ширине шины»: лучше закрепить через платформенный typedef + проверку lock-free (`static_assert(std::atomic<heartbeat_word_t>::is_always_lock_free)` где возможно). Тогда требование будет не декларацией, а машинно проверяемым контрактом.

[сессия 0011][Кодекс] В `working_technology.md` остаются несинхронизированные места относительно принятого решения: сейчас там всё ещё old-flow с `sort` в pre-seal и без явной двухфазной привязки heartbeat. Нужна синхронизация, иначе у команды будет два разных процесса инициализации.

[сессия 0011][Кодекс] В `sys_registry_structure.md` уже обновлена модель heartbeat, но стоит ещё явно дописать API-строку уровня интерфейса, например `registry.add(task_descriptor, task_wrapper)` или `registry.bind_wrapper(task_id, wrapper)` — чтобы термин «связка при регистрации» был отражён не только в описательном тексте, но и в контракте API.


согласен с инвариантом `step()` запрещён до `attach_hb()` 

Кодекс синхронизировал sys_registry_structure.md и sys_registry_structure.md

закрепить через платформенный typedef + проверку lock-free (`static_assert(std::atomic<heartbeat_word_t>::is_always_lock_free)` где возможно) - Я согласен. Клод - твое мнение????

[сессия 0011][клод]

1) По static_assert lock-free — **полностью поддерживаю**. Аргументы:

   На встроенных MCU с фиксированной архитектурой (Cortex-M4, например) `is_always_lock_free` для нативного слова всегда `true` на этапе компиляции — это не runtime-проверка, а именно статическая гарантия. `static_assert` делает это нарушение ошибкой компиляции, а не UB или замедлением в runtime. Формулировка:
   ```cpp
   using heartbeat_word_t = uint32_t; // один typedef на платформу
   static_assert(std::atomic<heartbeat_word_t>::is_always_lock_free,
       "heartbeat_word_t must be lock-free atomic on this platform");
   ```
   Это C++17+. Если проект на C++14 — альтернатива: `std::atomic<T>{}.is_lock_free()` в runtime assert при старте, но это слабее. Рекомендую зафиксировать C++17 как минимальный стандарт для этой части.

2) По инварианту `step()` до `attach_hb()` — согласен с Кодексом и оператором. Реализацию предлагаю через **debug-only assert** без runtime-стоимости в release:
   ```cpp
   void step(uint32_t now) noexcept {
       assert(hb_ != nullptr); // только в debug
       payload_.step(now);
       hb_->store(now, std::memory_order_release);
   }
   ```
   Хранить `hb_` как указатель (`atomic<heartbeat_word_t>*`), инициализированный `nullptr`. `attach_hb()` устанавливает указатель. В release-сборке assert вырезается — zero-cost. Это проще чем флаг `hb_attached_` и даёт ту же защиту.

3) По типу `hb_` в wrapper — с переходом на `attach_hb()` конструктор wrapper меняется: второй аргумент `std::atomic<uint32_t>& hb` убирается. `hb_` становится указателем, инициализированным в `nullptr`. Это изменение `taskwrapper.hpp`. Нужно отразить в `wrapper_structure.md`.

4) По синхронизации `working_technology.md` — Кодекс прав что там ещё old-flow. Но сейчас `working_technology.md` актуализирован коллегой (шаги 1-7 в init(), registry.add(desc, wrapper)). Проверю и подтвержу после текущей записи.

5) **Итоговый статус документов после сессий 0009-0011:**
   - `sys_registry_structure.md` — синхронизирован ✓ (Кодекс)
   - `working_technology.md` — синхронизирован ✓ (Кодекс)
   - `wrapper_structure.md` — **НЕ синхронизирован**: раздел 4 (конструктор) и раздел 5 (heartbeat) ещё описывают старую модель с `hb` в конструкторе. Это единственный оставшийся несинхронизированный документ.


   [сессия 0012][оператор]

   По static_assert lock-free - консенсус.
   По инварианту `step()` до `attach_hb()` - консенсус.
   Клод обновил wrapper_structure.md


   Вопрос идентифицируемости задач в реестре. трех-четырех-пяти символьные названия? Где прописываются? Во враппере? как прописываются???? Нужно понимать что может быть ДВА экземпляра враппера с одинаковым функционалом (два разных нагревателя в пивоваране управляются разными задачами одного класса) 

[сессия 0013][Кодекс] По вопросу идентифицируемости задач: идентичность должна жить в `TaskDescriptor`, не во wrapper и не в payload-классе. Wrapper может быть одинакового типа для нескольких задач, это нормально.

[сессия 0013][Кодекс] Предлагаю разделить два поля: `task_key` (машинный уникальный ключ, compile-time/enum/uint16) и `task_name` (человекочитаемая короткая метка для отчётов/логов). Уникальность проверяется по `task_key`, а не по `task_name`.

[сессия 0013][Кодекс] `task_name` можно ограничить фиксированной длиной (например, 4-8 ASCII символов) и хранить как `fixed_string<N>`/`char[N]` в descriptor. Это удобно для embedded и детерминированно по памяти.

[сессия 0013][Кодекс] Для кейса «два нагревателя одного класса» решение такое: два разных descriptor с разными `task_key`/`task_name` (например, `HEAT_A`, `HEAT_B`), но могут ссылаться на один и тот же тип wrapper/payload-класса. Тип не является идентификатором экземпляра.

[сессия 0013][Кодекс] Рекомендую добавить инвариант seal-time: `task_key` уникален в реестре; дублирование — ошибка конфигурации. В `registry_report.tasks` выводить и `task_key`, и `task_name`.


[сессия 0014][Оператор]

Предлагаю разделить два поля: `task_key` (машинный уникальный ключ, compile-time/enum/uint16) и `task_name` (человекочитаемая короткая метка для отчётов/логов). Уникальность проверяется по `task_key`, а не по `task_name`.

Хм. Хорошее решение, пока мы не приходим к контроллеру СТМ32Ф1 с 20К памяти. Какие еще есть варианты?

Понятно, что идентификатор задачи (по крейней мере человекочитаемый) - поле экземпляра задачи. 

Рекомендую добавить инвариант seal-time: `task_key` уникален в реестре; дублирование — ошибка конфигурации. - да, дублирование иднтификаторов в реестре - ошибка конфигурации и должно падать при попытке регистрации задачи с уже зарегистрированным идентификатором.

[сессия 0015][клод]

1) По вопросу памяти на STM32F1 (20 KB RAM). Ключевое разделение: строковые литералы в embedded системах живут во flash (.rodata), а не в RAM. Указатель `const char*` на ROM-строку стоит 4 байта RAM. Само строковое содержимое — ноль байт RAM.

   Вариант A — `task_id` (uint8_t) + `task_name` (`const char*` → flash):
   - 8 задач × (1 + 4) байт = 40 байт RAM. Приемлемо.
   - Имя живёт во flash, не в дескрипторе. Нет накладных расходов на RAM от длины строки.

   Вариант B — только `task_id` (uint8_t), без имени:
   - 8 задач × 1 байт = 8 байт RAM.
   - Имена — только в исходном коде (C++-символы). В runtime имени нет.
   - `task_name = nullptr` everywhere.

   Вариант C — `task_name` (`const char*`) без отдельного `task_id`:
   - Уникальность по содержимому строки (сравнение при `registry.add()`).
   - Одно поле вместо двух, но сравнение строк при регистрации.
   - Адрес строки как ключ ненадёжен (может совпасть при оптимизации линковщика).

2) Мой вывод — рекомендую вариант A с опциональным именем:
   - `task_id`: `uint8_t`, назначается разработчиком (например, enum-значение), уникальный, минимальный по памяти.
   - `task_name`: `const char*`, может быть `nullptr`. В ultra-tight сборках: `#define STAM_NO_TASK_NAMES` убирает поле полностью или заменяет на `nullptr` константой.
   - Уникальность проверяется по `task_id`, не по `task_name`.

3) Оператор уточнил: проверка дублирования — при `registry.add()`, не при `seal()`. Согласен и поддерживаю: fail-fast, до заморозки реестра. Это меняет формулировку инварианта Кодекса: не «seal-time», а «add-time».

4) Ключевой принцип по идентификации: тип payload-класса ≠ идентификатор задачи. `TaskDescriptor` — идентификатор экземпляра. Два дескриптора одного типа payload (HEAT_A, HEAT_B) — корректная конфигурация. Wrapper знает, что делать; реестр знает, кто есть кто.

5) Итоговое предложение для `TaskDescriptor` v1:
   ```
   task_id    : uint8_t      // уникальный ключ, задаётся разработчиком
   task_name  : const char*  // человекочитаемое имя → flash, может быть nullptr
   ```
   Инвариант: `task_id` уникален в реестре, проверяется при `registry.add()`, дублирование = немедленная ошибка регистрации.




[сессия 0014][Оператор]

Соглвсен. Кодекс????
[сессия 0014][Кодекс] Зафиксирую главный технический риск: документы уже перешли на двухфазный wrapper (`TaskWrapper(payload)` + `attach_hb()`), но `taskwrapper.hpp` всё ещё в старом виде (`TaskWrapper(payload, hb_ref)`). Пока код не синхронизирован, архитектурный контракт не исполняется.

[сессия 0014][Кодекс] Предлагаю считать это blocker для закрытия v1-документов: либо быстро обновить `taskwrapper.hpp`, либо явно пометить в документах статус `planned/not implemented`, чтобы не создавать ложную уверенность у команды.

[сессия 0014][Кодекс] По идентифицируемости задач поддерживаю модель `task_key + task_name`; дополнительно рекомендую запретить изменение `task_key` после `add()` (и тем более после `seal()`), иначе можно сломать привязки отчётов/диагностики между ревизиями.

[сессия 0014][Кодекс] Для человекочитаемости отчётов полезно зафиксировать формат `task_name`: только `[A-Z0-9_]`, длина 4..8. Это убирает хаос в логах и делает diff отчётов стабильнее.

[сессия 0014][Кодекс] Для случая двух одинаковых экземпляров рекомендую правило именования: `<FUNC>_<INSTANCE>` (пример: `HEAT_A`, `HEAT_B`) и отдельно стабильный `task_key` из enum/ID-таблицы. Тогда одинаковый payload-класс не мешает трассировке и safety-диагностике.

[сессия 0015][опреатор]
Файл taskwrapper.hpp - примерная структура обертки для задачи. Может быть использована, но не является догмой.


модель `task_key + task_name` - консенсус.

запретить изменение `task_key` после `add()` - согласен

`<FUNC>_<INSTANCE>` (пример: `HEAT_A`, `HEAT_B`) - решение об именовании принимает разработчик. Не нашего ума дело. пусть хоть ТАСК1, ТАСК2, ТАСК3 называет. У него есть бумажка. На бумажке есть схема, в схеме есть имена инит должет читаться как эта схема . НЕ жестим.


Клод???

[сессия 0016][клод]

1) По taskwrapper.hpp как примерной структуре — принял. Документы описывают архитектурный контракт; файл .hpp — ориентир для реализации, не обязательный к соблюдению. Blocker-а нет. Когда дойдёт до реализации — сверятся с wrapper_structure.md.

2) По консенсусу task_key + task_name — фиксирую итог:
   - `task_key`: малый целый тип (предлагаю `uint8_t`), задаётся разработчиком, уникален в реестре, неизменен после `registry.add()`.
   - `task_name`: `const char*` → flash, опциональна (nullptr допустимо), формат — **на усмотрение разработчика**, реестр не регламентирует.
   - Уникальность проверяется по `task_key` при `registry.add()`. Дублирование = немедленная ошибка.

3) По именованию — полностью согласен с оператором. Конвенция именования — зона ответственности разработчика и его бумажной схемы. Реестр отвечает за уникальность ключа, не за семантику имён. Это важный принцип: реестр — механизм, а не методолог.

4) По инварианту неизменности task_key после add(): согласен. Практическая реализация — `task_key` как `const` поле в дескрипторе (задаётся при конструировании, не меняется). Типовая система сама закрывает этот инвариант без дополнительных проверок.

5) Полезный паттерн для разработчика (не правило, а наблюдение): task_key удобно задавать через `enum class TaskId : uint8_t { ... }`. Тогда и машинный ключ, и читаемое имя в коде — одно место. Но это выбор команды.

6) Итоговые поля идентичности для TaskDescriptor v1:
   ```
   task_key  : uint8_t      // уникальный ключ экземпляра, const, задаётся разработчиком
   task_name : const char*  // метка → flash, nullptr допустимо, формат свободный
   ```

[сессия 0016][Оператор]

По наименованию вроде полный консенсус. Кодекс, проверь еще раз. И если все ок, подтверди консенсус.

[сессия 0017][Кодекс] Перепроверил обсуждение и текущие файлы: по наименованию задач консенсус есть. Подтверждаю.

[сессия 0017][Кодекс] Финальная формулировка консенсуса:
- `task_key` (или `task_id`, выбрать единый термин) — машинный уникальный ключ задачи, проверяется при `registry.add()`, после add неизменяем.
- `task_name` — человекочитаемая метка, опциональна, формат/конвенция полностью на стороне разработчика.
- Реестр валидирует уникальность ключа, но не навязывает стиль именования.

[сессия 0017][Кодекс] Рекомендую только убрать терминологический дрейф: сейчас в документах встречаются и `task_key`, и `task_id`. Для чистоты контрактов лучше оставить одно имя везде (я за `task_id`, т.к. уже зашито в `sys_registry_structure.md` и `working_technology.md`).

[сессия 0017][Кодекс] По wrapper/heartbeat сейчас технически всё согласовано с документами: двухфазная привязка (`TaskWrapper(payload)` + `attach_hb`), lock-free typedef и assert-защита `step()` до привязки hb уже отражены в коде.


[сессия 0017][оператор]

Ок. Везде использеум task_id.

Полностью ли покрыты вопросы архитектуры враппера и сисстемного регистра? Что забыли??

[сессия 0018][клод]

Сверил все документы и обновлённые файлы. Статус кода: docs/concepts/ — синхронизированы Кодексом (tags.hpp, taskwrapper.hpp корректны). Реальные stam-rt-lib/include/ — старые, но это допустимо: оператор обозначил их как примерные структуры.

Что покрыто — полно и согласовано:
- концепты Steppable / RtSafe / RtHooks / RtPayload
- единый TaskWrapper<Steppable>, RT-граница на уровне registry.add()
- двухфазная инициализация hb: TaskWrapper(payload) → attach_hb() при registry.add()
- heartbeat_word_t + static_assert(is_always_lock_free)
- step() запрещён до attach_hb() — assert(hb_ != nullptr)
- attach_hb() ровно один раз — assert(hb_ == nullptr) в attach_hb()
- memory_order: release при записи, acquire при чтении монитора
- unsigned timeout formula
- task_id (uint8_t, const, unique, add-time check) + task_name (const char*, свободный формат)
- seal() порядок шагов, инварианты, сигнальная маска, registry_report

Нашёл три непокрытых вопроса:

**1) Gap: неnaблюдаемые задачи (observable == false) + assert(hb_ != nullptr)**

Текущий дизайн: реестр вызывает attach_hb() только если observable == true. Если observable == false — attach_hb() не вызывается, hb_ остаётся nullptr, step() падает по assert. Это противоречие.

Предлагаю: реестр вызывает attach_hb() для ВСЕХ задач при registry.add(), независимо от observable. Для non-observable задач heartbeat выделяется, но safety-монитор его не читает. Overhead: одна atomic-запись в step() + один slot в hb-массиве — приемлемо. Это убирает условную логику из step() и сохраняет инвариант assert.

**2) Открытый вопрос: TaskWrapperRef — что хранит реестр?**

В sys_registry_structure.md раздел 12 это всё ещё помечено как открытый вопрос. Для v1 нужно решение. Два варианта:
- `ITaskWrapper*` — виртуальный интерфейс (vtable), один указатель на объект. Простейшая реализация, накладные расходы — одна косвенная ветвь на вызов step().
- Таблица function pointer-ов в дескрипторе (C-style vtable): `void(*step_fn)(void*, uint32_t)` и т.д. — те же расходы, без виртуального наследования.
Оба рабочие для embedded. Для v1 рекомендую ITaskWrapper* — проще в реализации.

**3) Открытый вопрос: ширина signal_mask_t**

В разделе 12 также. Для v1 достаточно uint32_t (≤ 32 задачи). Предлагаю зафиксировать SIGNAL_MASK_WIDTH = 32, signal_mask_t = uint32_t, оставить возможность расширить в v2.

Всё остальное — покрыто. Архитектура wrapper + реестр завершена по существу.

[сессия 0019][оператор]

**1) Gap: неnaблюдаемые задачи (observable == false) + assert(hb_ != nullptr)** - дыхалку аттачим всегда! А пользует ее задача - не пользует, это ее проблемы.


vtable - в РТ - виртуалы???? Не уверен.

**3) Открытый вопрос: ширина signal_mask_t** 
по ширине шины данных?






[сессия 0020][Кодекс] Перепроверил свежие комментарии. Подтверждаю: `attach_hb()` для всех задач — корректное и простое решение, снимает ветвление в `step()` и убирает класс ошибок для non-observable задач.

[сессия 0020][Кодекс] По `TaskWrapperRef` в RT-контексте поддерживаю опасение оператора насчёт virtual. Для v1 предлагаю зафиксировать безвиртуальный type-erasure: в дескрипторе хранить `void* obj` + набор function pointer (`step_fn`, `init_fn`, `alarm_fn`, `done_fn`). Это deterministic, без RTTI/vtable, без heap, с предсказуемым вызовом.

[сессия 0020][Кодекс] По `signal_mask_t`: «по ширине шины» — полезный ориентир, но формально критерий должен быть такой: минимальный тип, который покрывает `MAX_TASKS` и поддерживает быстрые битовые операции на целевой платформе. Для текущего embedded v1 рекомендую фиксировать `uint32_t` и `SIGNAL_MASK_WIDTH = 32`.

[сессия 0020][Кодекс] Если задач станет >32, лучше переходить не на одиночный `uint64_t`, а на chunked-маску (`std::array<uint32_t, NWORDS>`), чтобы сохранить платформенную предсказуемость на 32-бит MCU.

[сессия 0020][Кодекс] Итог: архитектурно остаются только 2 решения для явной фиксации в документах v1: (1) `TaskWrapperRef` = function-table type-erasure (без virtual), (2) `signal_mask_t` = `uint32_t`, `SIGNAL_MASK_WIDTH=32`.

[сессия 0021][клод]

1) По attach_hb() для всех задач — полный консенсус. Фиксирую: реестр вызывает attach_hb() для каждой задачи при registry.add() независимо от observable. Wrapper не знает и не спрашивает — он просто всегда имеет валидный hb_. Монитор смотрит hb только у задач с observable == true. Чисто и без ветвления.

2) По vtable в RT — поддерживаю опасение оператора и предложение Кодекса. Уточнение: `virtual` в RT технически детерминирован на MCU без кэша (Cortex-M0/M3/M4 без D-кэша — один косвенный переход, фиксированное число тактов). Но явная function-table предпочтительнее по трём причинам:
   - нет зависимости от C++ ABI и расположения vtable в памяти
   - таблица функций может быть const и жить в flash — vtable тоже, но неявно
   - явный контроль над тем, что именно вызывается, без компиляторной магии
   Поддерживаю вариант Кодекса: структура с `void* obj` + четыре function pointer. Для v1 это проще и читаемее, чем наследование.

3) По signal_mask_t — «по ширине шины» — правильный принцип, и он согласуется с уже принятым heartbeat_word_t. Та же логика: один нативный слово — одна атомарная операция. Для v1 на 32-битном MCU: `signal_mask_t = uint32_t`, `SIGNAL_MASK_WIDTH = 32`. Добавить `static_assert(MAX_TASKS <= SIGNAL_MASK_WIDTH)` — аналогично static_assert для lock-free. Если когда-нибудь задач станет больше 32 — это решение v2, как верно отметил Кодекс (chunked mask).

4) Итог по открытым вопросам из раздела 12 sys_registry_structure.md:
   - `TaskWrapperRef` = function-table: `{ void* obj; step_fn; init_fn; alarm_fn; done_fn }`
   - `signal_mask_t = uint32_t`, `SIGNAL_MASK_WIDTH = 32`
   Оба вопроса готовы к закрытию. Раздел 12 можно обнулить или убрать после внесения этих решений в основной текст.


[сессия 0020][Оператор]

Итого вроде все вопросы закрыты.

Все ли готово для внесения изменений в документы?

[сессия 0022][Кодекс] По вопросу оператора «готово ли для внесения изменений»: почти готово, но вижу 3 точечных рассинхронизации в документах, их лучше закрыть одним проходом.

[сессия 0022][Кодекс] 1) `task_name` тип: в обсуждении консенсус на `const char*` (flash, опционально), а в `sys_registry_structure.md` сейчас `char[9]?`. Нужно выбрать один вариант. Для STM32-памяти логичнее `const char*`.

[сессия 0022][Кодекс] 2) Heartbeat для всех задач: в обсуждении принято «attach всегда», но в `sys_registry_structure.md` ещё формулировка `hb_ref (для observable задач)`. Это противоречие. Предлагаю: `hb_ref` есть у каждой задачи; safety-монитор проверяет только `observable == true`.

[сессия 0022][Кодекс] 3) Раздел открытых вопросов в `sys_registry_structure.md` всё ещё содержит два уже решённых пункта (`TaskWrapperRef` и `signal_mask_t`). После фиксации: `TaskWrapperRef = function-table type erasure`, `signal_mask_t = uint32_t`, `SIGNAL_MASK_WIDTH = 32`, раздел «Открытые вопросы» можно убрать или оставить пустым.

[сессия 0022][Кодекс] После этих трёх правок считаю пакет архитектурных решений v1 полностью закрытым и готовым к финализации документов.

[сессия 0023][клод]

Перепроверил sys_registry_structure.md и wrapper_structure.md. Все три точки Кодекса подтверждаю — они точные.

По каждой:

1) `char[9]?` → `const char*` в TaskDescriptor раздела 3 — да, противоречие с консенсусом. `const char*` правильно: строка в flash, nullptr допустимо, размер не фиксирован.

2) hb_ref «для observable задач» → hb_ref у каждой задачи — да, разделы 7 и 10 всё ещё несут старую формулировку. Правка: hb_ref присутствует в каждом TaskDescriptor; safety-монитор читает только там, где `observable == true`.

3) Раздел 12 «Открытые вопросы» → убрать или заменить на зафиксированные решения:
   - TaskWrapperRef = function-table: `struct { void* obj; step_fn; init_fn; alarm_fn; done_fn; }`
   - signal_mask_t = uint32_t, SIGNAL_MASK_WIDTH = 32, static_assert(MAX_TASKS <= SIGNAL_MASK_WIDTH)

Дополнительно: wrapper_structure.md раздел 5.2 не содержит явного «для всех задач». Стоит добавить одну фразу: «реестр вызывает attach_hb() для каждой задачи при registry.add(), независимо от observable».

Итог: четыре правки (три в sys_registry_structure.md + одна в wrapper_structure.md) — и архитектурные документы v1 закрыты. Готов вносить.


[сессия 0024][Кодекс] Финализировал итоговые документы по зафиксированному консенсусу.

[сессия 0024][Кодекс] Что закрыто в `sys_registry_structure.md`:
- `task_name` приведён к `const char*` (опционально, `nullptr` допустим).
- heartbeat описан как обязательный для всех задач; `observable` влияет только на то, читает ли монитор timeout.
- `signal_mask_t` и `SIGNAL_MASK_WIDTH` зафиксированы как `uint32_t` и `32` (+ `static_assert(MAX_TASKS <= SIGNAL_MASK_WIDTH)`).
- раздел «Открытые вопросы» заменён на «Зафиксированные решения v1» с явной структурой `TaskWrapperRef` через function-table (`void* + fn ptr`).

[сессия 0024][Кодекс] Что закрыто в `wrapper_structure.md`:
- явно добавлено, что реестр вызывает `attach_hb()` для каждой задачи при `registry.add()`, независимо от `observable`.

[сессия 0024][Кодекс] Дополнительно синхронизировал `working_technology.md` по формулировке `task_name` (убран жёсткий формат длины, формат имени — на стороне разработчика).

[сессия 0024][Кодекс] На этом этапе считаю пакет v1-документов финализированным и согласованным между собой.

[сессия 0025][клод]

Проверил финальные версии обоих документов. Картина хорошая — все консенсусные решения отражены. Нашёл три точечных несогласованности:

**1) Структурная: `hb_ref` отсутствует в TaskDescriptor (раздел 3 sys_registry_structure.md)**

Раздел 3 показывает поля дескриптора — там есть `wrapper_ref`, но нет `hb_ref`. Раздел 10 при этом пишет `TaskDescriptor.hb_ref → atomic<heartbeat_word_t>&`. Разрыв между определением и использованием. Нужно добавить `hb_ref` в struct в разделе 3.

**2) Терминологическая: раздел 10 использует `uint32_t`, а не `heartbeat_word_t`**

Строка: `"проверка таймаута: (now - last_hb) > timeout_ticks в uint32_t арифметике"`. После принятия `heartbeat_word_t` как платформенного typedef правильнее: `в heartbeat_word_t арифметике`. Мелко, но для консистентности документа стоит исправить.

**3) Поле `name` у канала в registry_report (раздел 11)**

Секция tasks отчёта выводит `task_id`, `task_name` — понятно откуда. Но секция channels показывает поле `name` для канала — а в `ChannelDescriptor` (раздел 4) поля `name` нет. Либо нужно добавить `name : const char*` в ChannelDescriptor (по аналогии с TaskDescriptor), либо убрать из секции channels отчёта.

Всё остальное — согласовано. Три мелкие правки, одна структурная (п.1).

[сессия 0026][клод] Внёс три правки в sys_registry_structure.md:

1. **TaskDescriptor (раздел 3)** — добавлено поле:
   `hb_ref : atomic<heartbeat_word_t>&  // heartbeat (системное владение, присутствует у каждой задачи)`

2. **Раздел 10** — формула таймаута:
   `uint32_t арифметике` → `heartbeat_word_t арифметике`

3. **ChannelDescriptor (раздел 4)** — добавлено поле:
   `name : const char*  // человекочитаемая метка (flash), опционально (nullptr допустим)`
   Это согласует определение канала с использованием `name` в registry_report (раздел 11).

Архитектурные документы v1 закрыты и внутренне согласованы.

[сессия 0027][Кодекс] Перепроверил последние правки и выводы коллег: архитектурный пакет v1 сейчас действительно выглядит закрытым. Ключевые спорные пункты (task_id/task_name, attach_hb для всех задач, TaskWrapperRef без virtual, signal_mask=uint32_t) зафиксированы в документах.

[сессия 0027][Кодекс] Дополнительная микро-рекомендация для чистоты интерфейсов: в `TaskWrapperRef.step_fn` и смежных контрактах ввести единый `tick_t` typedef (сейчас местами голый `uint32_t`). Это не блокер, но уберёт дрейф типов при возможной эволюции платформы.

[сессия 0027][Кодекс] С точки зрения архитектуры — новых незакрытых вопросов не вижу. Дальше уже этап реализации/валидации (code path + unit/integration checks), а не пересборки контрактов.

[сессия 0028][Кодекс] Внёс правку по моему замечанию о дрейфе типов времени: в документах введён единый `tick_t` и применён в callback-контрактах.

[сессия 0028][Кодекс] Что синхронизировано:
- `sys_registry_structure.md`: в базовых типах зафиксирован `tick_t = uint32_t`; в `TaskWrapperRef` сигнатура `step_fn(void*, tick_t)`.
- `wrapper_structure.md`: `Steppable` и `step(now)` переведены на `tick_t`; в типах heartbeat зафиксировано `using tick_t = heartbeat_word_t`.

[сессия 0028][Кодекс] Это закрывает расхождение `uint32_t` vs алиас времени и упрощает будущую смену платформенного тикового типа в одном месте.

[сессия 0029][клод]

Проверил изменения. Всё корректно.

1) `tick_t = heartbeat_word_t` — правильное решение, не просто удобство. `step()` записывает `now` напрямую в `hb_`: `hb_->store(now, release)` — значит типы должны совпадать по контракту. Алиас делает это явным и убирает скрытый cast из docs/taskwrapper.hpp. Одобряю.

2) Бонус-эффект: `static_assert(is_always_lock_free)` на `heartbeat_word_t` теперь неявно гарантирует lock-free и для тикового значения в heartbeat — что правильно для RT.

3) Раздел 9 sys_registry_structure.md: три typedef все = `uint32_t` выглядит избыточно, но это правильно — разные семантические роли закреплены именами. При смене платформы меняется одно место, не три.

4) Единственное замечание: `tick_t` объявлен в wrapper_structure.md как `using tick_t = heartbeat_word_t`, а в sys_registry_structure.md как `tick_t = uint32_t`. Нужно убедиться, что в реализации `tick_t` объявляется один раз и в одном header — иначе при смене `heartbeat_word_t` надо будет менять оба места. Для документов это нестрого, но для кода стоит зафиксировать: `tick_t` живёт в `tags.hpp` как `using tick_t = heartbeat_word_t`.

Итого: архитектура v1 закрыта полностью. Документы согласованы.

[сессия 0031][Кодекс] Принял замечание оператора по формулировке `attach_hb`. Поправил документы, чтобы убрать двусмысленность «кто именно вызывает».

[сессия 0031][Кодекс] Новая формулировка в контрактах: `wrapper.attach_hb(...)` вызывается внутренней логикой функции `registry.add(...)` (в фазе регистрации), а не внешним кодом и не «реестром абстрактно».

[сессия 0031][Кодекс] Синхронизированы формулировки в `wrapper_structure.md` (интерфейс, lifecycle, инварианты) и `sys_registry_structure.md` (раздел интерфейса add).


[сессия 0031][Оператор]

Инит враппера должен происходить ДО сиал(). После сиал() начинается рт-жизнь.

[сессия 0032][Кодекс] Поддерживаю замечание оператора: `wrapper.init()` должен выполняться ДО `seal()`. После `seal()` начинается RT-жизнь (scheduler/runtime), и инициализационные хуки туда не должны попадать.

[сессия 0032][Кодекс] Это требует синхронизации текстов:
- в `wrapper_structure.md` lifecycle сейчас показывает `seal()` перед `init()` — порядок нужно инвертировать;
- в `working_technology.md` в последовательность `init()` добавить явный шаг `init wrappers` перед `seal()`;
- в `sys_registry_structure.md` в разделе `seal()`/post-seal явно закрепить, что `init()` хуки не являются частью RT-фазы.

[сессия 0032][Кодекс] Предлагаемый контракт v1: `registry.add(...) -> wrapper.attach_hb(...) -> wrapper.init() -> registry.seal() -> runtime step/alarm/done`. Это полностью снимает двусмысленность по границе bootstrap vs RT.

[сессия 0033][Кодекс] Исправил документы по замечанию оператора: `wrapper.init()` перенесён в bootstrap-фазу ДО `seal()`.

[сессия 0033][Кодекс] Что изменено:
- `wrapper_structure.md`: lifecycle переставлен в порядок `add/attach_hb -> init -> seal -> runtime step`; добавлен инвариант, что `init()` выполняется до `seal()`.
- `working_technology.md`: в структуру `init()` добавлен явный шаг `Вызов wrapper.init()` перед `seal()`; в полном примере добавлены вызовы `task_*_wrapper.init()`; в разделе `seal()` добавлено предусловие и в таблице lifecycle добавлен `wrapper.init` в pre-seal.
- `sys_registry_structure.md`: в разделе `seal()` добавлено предусловие `wrapper.init() до seal()` и явная фиксация, что после `SEALED` `init()`-хуки в RT-фазе не вызываются.

[сессия 0033][Кодекс] Итого: замечание «инит враппера до сиал» отражено в трёх ключевых документах, противоречий по фазам bootstrap/RT больше не осталось.


[сессия 0033][оператор]


TaskDescriptor {
    task_id      : uint8_t       // уникальный ключ задачи, задаётся разработчиком
    task_name    : const char*   // человекочитаемая метка (flash), опционально (nullptr допустим)
    task_index   : uint          // присваивается при seal(), = signal_bit
    signal_bit   : uint          // = task_index (v1 constraint)
    core_id      : CoreId        // ядро исполнения, явно задаётся разработчиком
    priority     : uint8_t       // статический приоритет
    task_type    : TaskType      // Periodic | SignalDriven (выводится)
    period_ticks : uint32_t?     // только Periodic, ≥ 1
    phase_ticks  : uint32_t?     // только Periodic, < period_ticks
    observable   : bool          // нужна ли watchdog-наблюдаемость
    timeout_ticks: uint32_t?     // только если observable == true
    wrapper_ref  : TaskWrapperRef              // ссылка на TaskWrapper задачи
    hb_ref       : atomic<heartbeat_word_t>&  // heartbeat (системное владение, присутствует у каждой задачи)
    ports        : PortList                   // типизированные порты (InPort / OutPort)
}



signal_bit   : uint          // = task_index (v1 constraint)
Парни, я понимаю, что у вас памяти - хоть в задницу пихай, но байт вместо бита мне не кажется хорошим решением. 


task_index   : uint - и сколько задач у нас туда поместится?

task_type    : TaskType      // Periodic | SignalDriven (выводится) 
 - а это нахуа? 

 period_ticks : uint32_t?     // только Periodic, ≥ 1
≥ 1?????? Если  = 0 здача непериодическая. 

phase_ticks  : uint32_t?     // только Periodic, < period_ticks
зачем?


[сессия 0034][клод]

Четыре замечания оператора — все по делу. Разбираю по порядку.

**1) `task_index : uint` и `signal_bit : uint` — тип избыточен**

`uint` на 32-битном MCU = 4 байта. Для значений 0..31 (MAX_TASKS=32) это расточительство.
`signal_bit` — позиция бита в signal_mask_t (uint32_t), значения 0..31. Хватит `uint8_t` (0..255).
`task_index` — то же самое: порядковый номер задачи после сортировки, ≤ 31.
Оба заменить на `uint8_t`. При SIGNAL_MASK_WIDTH=32 максимальное значение — 31, в `uint8_t` влезает с запасом.

Предлагаю:
```
task_index : uint8_t    // присваивается при seal(); 0..(MAX_TASKS-1)
signal_bit : uint8_t    // = task_index (v1 constraint); позиция бита в signal_mask_t
```

**2) `task_type : TaskType` — убрать**

Поддерживаю вопрос оператора. `task_type` — производное поле, вычисляется тривиально из `period_ticks`.
Хранить его отдельно — нарушение single source of truth. При `period_ticks = 0` → SignalDriven, иначе → Periodic.
Вывод: убрать `task_type` из дескриптора. Если нужен convenience-метод — `is_periodic() { return period_ticks > 0; }` — но не поле.

**3) `period_ticks ≥ 1` → семантика нуля**

Оператор предлагает чище: `period_ticks = 0` означает «непериодическая», `period_ticks ≥ 1` — периодическая.
Это снимает nullable (`?`) и убирает `task_type` одним ходом. Поддерживаю.

Новая семантика:
```
period_ticks : uint32_t  // 0 = SignalDriven; ≥ 1 = Periodic (тики периода)
```
Контракт: `period_ticks` всегда присутствует в дескрипторе. Значение 0 — маркер SignalDriven. Никакого `?`, никакого `task_type`.

**4) `phase_ticks` — зачем**

Назначение: сдвиг первого запуска периодической задачи относительно t=0.
Пример: две задачи с period=100, без phase — обе запускаются в t=0, t=100, t=200 — одновременно, пик нагрузки.
С фазой: задача A в t=0, задача B в t=50 — нагрузка равномерна (phase spreading).

Нужно ли это в v1? Вопрос к оператору. Моё мнение: если нет явного требования — убрать из v1. Это нетривиальная фича планировщика, и в минималистичной системе всё стартует в t=0. Добавить в v2 при необходимости.

Если убирать `phase_ticks` из v1, то дескриптор упрощается до:
```
period_ticks : uint32_t  // 0 = SignalDriven; ≥ 1 = Periodic
```
Планировщик запускает все Periodic задачи с t=0.

**Итог по дескриптору (предлагаемая v1-структура):**
```
TaskDescriptor {
    task_id      : uint8_t             // уникальный ключ, задаётся разработчиком
    task_name    : const char*         // метка → flash, nullptr допустимо
    task_index   : uint8_t            // присваивается при seal()
    signal_bit   : uint8_t            // = task_index (v1 constraint)
    core_id      : CoreId             // ядро исполнения
    priority     : uint8_t            // статический приоритет
    period_ticks : uint32_t           // 0 = SignalDriven; ≥ 1 = Periodic
    observable   : bool               // нужна ли watchdog-наблюдаемость
    timeout_ticks: uint32_t           // только если observable == true; иначе 0 или ignored
    wrapper_ref  : TaskWrapperRef     // ссылка на TaskWrapper задачи
    hb_ref       : atomic<heartbeat_word_t>&  // heartbeat (системное владение, всегда присутствует)
    ports        : PortList           // типизированные порты (InPort / OutPort)
}
```
Убрано: `task_type`, `phase_ticks`. Тип исправлен: `task_index`/`signal_bit` → `uint8_t`.

Жду подтверждения по `phase_ticks` и `timeout_ticks` (тип и представление нуля — или тоже sentinel?).


[сессия 0033][оператор]


    task_id      : uint8_t             // уникальный ключ, задаётся разработчиком
    task_name    : const char*         // метка → flash, nullptr допустимо
    task_index   : uint8_t            // присваивается при seal()
    signal_bit   : uint8_t            // = task_index (v1 constraint)

    А вам не кажется, что у нас индексов задач примерно до хрена?

    Разработчик задает ТОЛЬКО имя задачи. И все такие уникальные, и все такие классные!

    Видимо пора проверить втруктуру на необходимость и достаточность.
[сессия 0035][Кодекс] По тезису оператора «проверить структуру на необходимость и достаточность» — поддерживаю. Предлагаю зафиксировать минимальный v1-контракт: разработчик задаёт только `task_name`, `core_id`, `priority`, `period_ticks`, `observable`, `timeout_ticks`, `ports`, `wrapper_ref`; всё производное (`task_index`, `signal_bit`, runtime-ссылки) формирует реестр.

[сессия 0035][Кодекс] Если выбираем модель «разработчик задаёт только имя», нужен жёсткий add-time инвариант: `task_name` уникально в реестре (строковое сравнение содержимого, не адреса). Тогда `task_id` как отдельное пользовательское поле можно убрать из v1 и не дублировать идентичность.

[сессия 0035][Кодекс] По замечанию про типы индексов: не использовать голый `uint` в контрактах. Ввести `task_index_t` и `signal_bit_t` как алиасы, завязанные на `MAX_TASKS`/`SIGNAL_MASK_WIDTH` (для текущего v1 достаточно `uint8_t`, но лучше фиксировать через typedef + static_assert, а не руками в каждом поле).

[сессия 0035][Кодекс] По `task_type` и `phase_ticks`: если цель v1 — минимализм, поддерживаю убрать `task_type` как производное и вынести/отложить `phase_ticks` в v2. Базовая семантика может быть `period_ticks == 0 -> SignalDriven`, `period_ticks > 0 -> Periodic`.

[сессия 0035][Кодекс] Практический компромисс v1: оставить структуру предельно плоской и детерминированной, но исключить все поля, которые 1:1 вычисляются из других или не используются runtime в первой реализации.

[сессия 0036][клод]

Оператор поставил правильный вопрос. Провожу аудит: разделяю поля на три категории.

**Категория A — задаёт разработчик:**
`task_name`, `core_id`, `priority`, `period_ticks`, `observable`, `timeout_ticks`, `ports`, `wrapper_ref`

**Категория B — формирует реестр при seal():**
`task_index` — позиция в отсортированном массиве / бит в signal_mask_t
`signal_bit` — = task_index (v1 constraint)

**Категория C — привязывает реестр при add():**
`hb_ref` — системный атомик heartbeat

**Избыточности в текущем дескрипторе:**

1. `task_id` + `task_name` — двойная идентичность. Если `task_name` уникален — `task_id` дублирует. Предлагаю: убрать `task_id`, `task_name` становится единственным пользовательским идентификатором. Но тогда `task_name` обязателен (не nullptr) и уникален. Проверка при add() — строковое сравнение содержимого, приемлемо (это bootstrap-код, не RT).

2. `task_index` + `signal_bit` — всегда равны в v1. Хранить оба — нарушение DRY. Убрать `signal_bit` как отдельное поле; зафиксировать в контракте: `signal_bit(task) == task_index`. Одно поле, одно значение.

3. `task_type` — производное от `period_ticks > 0`. Убрать.

4. `phase_ticks` — не в v1. Убрать.

**Итоговая минимальная структура v1:**
```
TaskDescriptor {
    // --- Разработчик задаёт ---
    task_name    : const char*        // уникальный идентификатор, обязателен (не nullptr)
    core_id      : CoreId            // ядро исполнения
    priority     : uint8_t           // статический приоритет
    period_ticks : uint32_t          // 0 = SignalDriven; ≥ 1 = Periodic
    observable   : bool              // нужна ли watchdog-наблюдаемость
    timeout_ticks: uint32_t          // если observable; иначе игнорируется
    ports        : PortList          // типизированные порты (InPort / OutPort)
    wrapper_ref  : TaskWrapperRef    // ссылка на TaskWrapper

    // --- Реестр присваивает ---
    task_index   : uint8_t           // при seal(); = позиция бита в signal_mask_t
    hb_ref       : atomic<heartbeat_word_t>&  // при registry.add()
}
```
Убрано: `task_id`, `signal_bit`, `task_type`, `phase_ticks`. Итого 4 поля убрано.

**По предложению Кодекса о `task_index_t`/`signal_bit_t`**: в v1 — избыточно. Голый `uint8_t` + `static_assert(MAX_TASKS <= SIGNAL_MASK_WIDTH)` закрывает тот же инвариант без дополнительного слоя typedef-ов. Для v2, если появятся chunked mask или расширенная адресация — да, тогда typedef нужен.

**Вопрос к оператору**: `task_name` становится обязательным (не nullptr) и уникальным. Принимаем? Это меняет ранее принятый консенсус (nullptr был допустим).


[сессия 0037][оператор]
`task_name` становится обязательным (не nullptr) и уникальным. - ДА. 

кто устанавливает сигналбит и как? Если только реестр знает какой задаче какой бит соответствует?


[сессия 0038][Кодекс] По вопросу оператора «кто устанавливает signal_bit и как»: `signal_bit` НЕ устанавливается разработчиком и не хранится как отдельная бизнес-сущность. Реестр присваивает `task_index` при `seal()`, а `signal_bit` в v1 определяется формулой `signal_bit = task_index`.

[сессия 0038][Кодекс] Кто ставит сам БИТ в маске ready/pending: runtime-слой (scheduler/ISR/сигнальные обработчики) через реестровую адресацию задачи. То есть сигнал «поставить задачу к запуску» идёт по индексу/дескриптору, а позиция бита берётся из уже зафиксированного `task_index`.

[сессия 0038][Кодекс] Практически это должно быть оформлено API уровня runtime, например: `set_pending(task_index)` или `set_pending(task_handle)`. Для кода, который знает только имя, lookup `task_name -> task_index` делается один раз в bootstrap, не в RT-цикле.

[сессия 0038][Кодекс] Важное замечание по консистентности: после последних обсуждений в документах остаётся терминологический дрейф (`task_id`/`task_name`, `task_type/phase_ticks` vs минимальный v1). Это отдельный cleanup-шаг, иначе часть текста противоречит текущему консенсусу.

[сессия 0039][Кодекс] Поддерживаю направление оператора: для v1 можно сделать `task_name` единственным пользовательским идентификатором (обязательный, уникальный, проверка уникальности при `registry.add()` по содержимому строки).

[сессия 0039][Кодекс] Тогда `task_id` из структуры можно убрать, чтобы не держать двойную идентичность. Это упрощает контракт и снижает количество обязательных полей для разработчика.

[сессия 0039][Кодекс] По вопросу signal bit: в v1 рекомендую не хранить отдельное поле `signal_bit`, а формально считать его функцией от `task_index` (`signal_bit(task) = task_index`). Реестр назначает `task_index` при `seal()`, runtime ставит pending-бит по этому индексу через API вида `set_pending(task_index)`/`set_pending(task_handle)`.

[сессия 0039][Кодекс] По необходимости/достаточности v1: поддерживаю убрать производные/необязательные поля (`task_type`, `phase_ticks`, отдельный `signal_bit`) и оставить минимальный набор, который реально нужен runtime и проверкам.

[сессия 0039][Кодекс] Технически сейчас в документах ещё есть следы старой модели (`task_id`, `task_type`, `phase_ticks`). Нужен один финальный cleanup-проход, чтобы тексты полностью соответствовали новому консенсусу 0037.

[сессия 0040][клод]

По вопросу оператора «кто устанавливает signal_bit» — добавлю к ответу Кодекса.

**Принципиальное разделение ответственности:**

| Кто | Что делает |
|-----|-----------|
| Реестр (seal()) | присваивает `task_index` каждой задаче |
| Runtime (scheduler/ISR) | ставит/снимает бит в `signal_mask_t` по `task_index` |
| Разработчик (bootstrap) | может один раз сохранить `task_index` через API реестра |
| Задача/Wrapper | НЕ знают своего `task_index` — не нужно |

Конкретная механика в RT: ISR или timer дёргает `set_pending(task_index)` — это атомарная OR-операция над `signal_mask_t`. Планировщик читает маску и запускает задачи по приоритету. Никаких имён, никаких строк — только битовые операции над uint32_t.

Если разработчику нужен handle до RT-фазы: `task_index_t idx = registry.find(task_name)` — один раз после `seal()`, результат сохраняется в bootstrap-переменной. В RT используется прямо `idx`, без lookup.

**По консенсусу cleanup:**

Полностью согласен с Кодексом. После подтверждения оператора готов провести один cleanup-проход по документам:

1. **`sys_registry_structure.md`** — TaskDescriptor: убрать `task_id`, `signal_bit`, `task_type`, `phase_ticks`; `task_name` — обязателен и уникален; добавить `registry.find(task_name) → task_index` в post-seal API.
2. **`working_technology.md`** — убрать `task_id` и `phase_ticks` из шаблона Task(); обновить пример.
3. **`wrapper_structure.md`** — убрать `task_id` из раздела registry_report; согласовать.

Один проход, все три документа. Команда оператора — и вношу.

**Итоговая минимальная структура (после cleanup):**
```
TaskDescriptor {
    // --- Задаёт разработчик ---
    task_name    : const char*        // уникальный идентификатор, обязателен
    core_id      : CoreId
    priority     : uint8_t
    period_ticks : uint32_t           // 0 = SignalDriven; ≥ 1 = Periodic
    observable   : bool
    timeout_ticks: uint32_t           // если observable; иначе игнорируется
    ports        : PortList
    wrapper_ref  : TaskWrapperRef

    // --- Реестр присваивает ---
    task_index   : uint8_t            // при seal(); = позиция бита в signal_mask_t
    hb_ref       : atomic<heartbeat_word_t>&  // при registry.add()
}
```


---

## [сессия 0041][Клод] — Отчёт о cleanup-проходе

Cleanup выполнен. Три файла приведены в соответствие с решениями сессий 0033–0040.

---

### Что изменено

**`sys_registry_structure.md`**

1. **TaskDescriptor** — удалены поля: `task_id`, `signal_bit`, `task_type`, `phase_ticks`. Структура разбита на две секции («Задаёт разработчик» / «Реестр присваивает»).
2. `task_name` — изменён статус с опционального на обязательный; уникален по содержимому строки.
3. `period_ticks` — убран nullable `?`; семантика: `0 = SignalDriven; ≥ 1 = Periodic`.
4. `timeout_ticks` — убран nullable `?`.
5. `task_index` — тип `uint` → `uint8_t`.
6. Удалён абзац про вывод `TaskType` из `period_ticks`.
7. Заменён абзац про `task_id`: теперь фиксируется статус `task_name` как единственного идентификатора, обязательного и уникального.
8. Уточнение `seal()` (раздел 7): `task_index` теперь `uint8_t`; `signal_bit` — derived constraint, не поле.
9. Инварианты (раздел 8): удалена строка `Periodic: фаза | phase_ticks < period_ticks`; строка уникальности разделена на два инварианта (`task_name` и канал).
10. Post-seal API (раздел 6.3): добавлен `registry.find(const char* task_name) → uint8_t`.
11. Сигнальная маска (раздел 9): уточнена формулировка `signal_bit` как производного, не хранимого поля.
12. Registry report (раздел 11): удалены `task_id` и `task_type` из списка полей секции tasks; добавлен `period_ticks`.
13. Раздел 6.1: проверка уникальности `task_id` → `task_name` (по содержимому строки).

**`working_technology.md`**

1. **Шаблон Task()** (раздел 3): удалены `task_id` и `phase_ticks`; `task_name` — первый аргумент, обязателен; комментарий к `period_ticks` изменён на `0 = SignalDriven; ≥ 1 = Periodic`.
2. Блок «Тип задачи» (раздел 3): переформулирован под sentinel-семантику.
3. Описание `signal_bit` (раздел 3): убрана пара `signal_bit + task_index`; оставлена формулировка v1 constraint.
4. Регистрация (раздел 6): `Дублирование task_id` → `Дублирование task_name`.
5. Инварианты (раздел 8): удалена строка `Periodic: фаза`; строка уникальности разделена.
6. Пример `init()` (раздел 9): удалены `task_id=...` из всех Task(...); `period_ticks=0` явно указан для SignalDriven задачи.
7. Registry report (раздел 13): удалены `task_id` и тип из полей секции tasks; добавлен `period_ticks`.

**`wrapper_structure.md`**

Правок не потребовалось: поля `task_id`, `task_type`, `phase_ticks`, `signal_bit` в файле отсутствовали.

---

### Что не менялось (намеренно)

- `taskwrapper.hpp` — уже синхронизирован, правок не требует.
- `tags.hpp` — не затронут cleanup-ом.
- `TaskWrapperRef` (раздел 12 `sys_registry_structure.md`) — function-table type erasure, без изменений.
- Семантика `hb_ref`, `heartbeat_word_t`, `attach_hb()` — без изменений.
- Порядок секций во всех файлах — сохранён.

---

### Открытые вопросы / v2

- `phase_ticks` убран из v1 полностью. Если фазовое смещение понадобится — возвращается в v2 как явный параметр с отдельным инвариантом.
- `registry.find(task_name)` — O(n) линейный поиск на bootstrap. Для v1 достаточно; если задач станет много — v2 может использовать хэш-таблицу.

[сессия 0040][Кодекс] Провёл сверку непротиворечивости итоговых файлов (`sys_registry_structure.md`, `wrapper_structure.md`, `working_technology.md`, `tags.hpp`, `taskwrapper.hpp`).

[сессия 0040][Кодекс] Что согласовано:
- единая модель bootstrap/RT (attach_hb и init до seal, после seal только RT-фаза);
- единая модель идентичности в docs: `task_name` как обязательный уникальный идентификатор;
- единая модель реестра по `TaskWrapperRef` (function-table) и signal mask (`uint32_t`, width=32).

[сессия 0040][Кодекс] Найденные противоречия/дрейф:
1) `tick_t` в docs vs `uint32_t` в коде.
   - docs (`wrapper_structure.md`/`sys_registry_structure.md`) используют `tick_t`.
   - `tags.hpp` не объявляет `tick_t`, `Steppable` всё ещё на `uint32_t`.
   - `taskwrapper.hpp::step` тоже `uint32_t`.
   Это не ломает идею, но нарушает «один тип времени» в контрактах.

2) `Steppable` концепт в docs и коде формально разошёлся по имени типа времени.
   - в docs: `step(tick_t now)`;
   - в `tags.hpp`: `step(uint32_t now)`.
   Нужна унификация в одном месте (`tags.hpp`), как и обсуждали ранее.

3) Дублирование источника типа времени в docs.
   - `sys_registry_structure.md` задаёт `tick_t = uint32_t` текстом,
   - `wrapper_structure.md` задаёт `using tick_t = heartbeat_word_t`.
   Для документов это допустимо, но по контракту реализации лучше оставить один canonical source: `tags.hpp`.

[сессия 0040][Кодекс] Рекомендованный фикс-пакет (минимальный):
- в `tags.hpp` добавить `using tick_t = heartbeat_word_t;` и перевести `Steppable` на `tick_t`;
- в `taskwrapper.hpp` заменить `step(uint32_t now)` на `step(stam::model::tick_t now)`;
- в docs оставить формулировку, что `tick_t` и `heartbeat_word_t` определяются в `tags.hpp` (без повторного «жёсткого» объявления в нескольких местах).

[сессия 0040][Кодекс] Итог: архитектурно пакет почти консистентен; остаётся один технический cleanup по единому типу времени (`tick_t`) между docs и кодом.

[сессия 0041][Кодекс] Внес правки по рекомендациям из проверки непротиворечивости (0040).

[сессия 0041][Кодекс] Изменения в коде:
- `tags.hpp`: добавлен канонический алиас `tick_t` (`using tick_t = heartbeat_word_t`), `Steppable` переведен на `tick_t`.
- `taskwrapper.hpp`: `TaskWrapper::step` переведен на `stam::model::tick_t`, убран лишний cast при записи heartbeat.

[сессия 0041][Кодекс] Изменения в документах:
- `sys_registry_structure.md`: в разделе сигнальной маски убрано дублирующее «жесткое» объявление типов времени; добавлена ссылка, что `tick_t/heartbeat_word_t` определяются в `model/tags.hpp`.
- `wrapper_structure.md`: в разделе типов зафиксировано, что канонические `tick_t/heartbeat_word_t` берутся из `stam::model` (`tags.hpp`).

[сессия 0041][Кодекс] Итог: дрейф `uint32_t` vs `tick_t` между кодом и docs устранён, источник типа времени теперь единый (`tags.hpp`).
